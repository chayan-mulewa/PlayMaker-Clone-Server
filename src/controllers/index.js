const authController = require("./auth.controller");
const materialContoller = require("./material.contoller");
const userController = require("./user.controller");

module.exports = { authController, materialContoller, userController };
